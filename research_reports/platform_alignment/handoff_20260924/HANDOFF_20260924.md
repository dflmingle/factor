# 换机交接单（2026-09-24）

旧机：`/data/games/factor_`。本单只写新机需要、而仓库里看不到的东西：**不在 git 里的本地缓存、
重建命令、平台状态、未决事项**。规则版本：
`full-a-qfq-label1-financialfix2-tieproxy1-pythonindex1-turnoverdiag1-qualitygate4`。

## 0. 新机第一天（按顺序）

```bash
git lfs install
git clone --recurse-submodules https://github.com/dflmingle/factor.git
cd factor
git lfs pull                                   # 约 541 MB 的本地数据快照
python scripts/local_recheck_data.py import --archive data/local_recheck/factor-local-recheck-data.tar.gz
python scripts/local_recheck_data.py verify
python scripts/start.py --login-if-needed      # 安装/升级 pandaai-cli 并引导登录（token 只留本机）
python scripts/handoff_check_20260924.py       # 本单配套自检：核对 5 个大缓存
```

第 5 步会告诉你哪些本地缓存缺失或与旧机不一致，并打印对应重建命令。
**不要**在没有核对前就跑依赖这些缓存的筛选脚本（会重建、慢，且结果会与新机当前数据状态绑定）。

## 1. 旧机环境（供比对）

| 项 | 值 |
| --- | --- |
| 工作目录 | `/data/games/factor_` |
| Python | `/home/minkefu/.conda/envs/ktransformers/bin/python`（3.11.14） |
| 关键库 | `torch 2.7.1+cu128`、`torchgfn==1.2.1`（GFN 需要）、`numpy/pandas/numba` |
| GPU | NVIDIA RTX 3090 24 GB（GP 用 `--device cuda:0`） |
| CPU / 内存 | 64 核 / 125 GB（宽字段 GP 很吃 CPU 侧数组缓存） |
| PandaAI CLI | `pandaai-cli 0.1.7`，`uv tool install` 落盘于 `/home/minkefu/.local/share/uv/tools/pandaai-cli` |
| 平台 token | `~/.pandaai/config.yaml`（**只在本机，不入库**） |
| 平台余额（2026-09-24 记录） | 1752.12 算力 |

显存小的新机：把 GP 的 `--field-cache-size` 调低（默认 24）；内存小的新机：
把 `--field-array-cache-size` 调低（默认 512）。两者都是纯本地性能开关，不影响结果口径。

## 2. 不在仓库里的数据（关键）

`.gitignore` 从 2026-09-24 起忽略 `*.pkl`（可重建的本地面板缓存，单个几百 MB）。
**只 `git clone` 拿不到下面这些文件**。二选一：

- **直拷（推荐）**：按 `local_data_manifest.json` 中的相对路径把文件拷到新机同一位置
  （rsync/U 盘/内网均可），再跑 `python scripts/handoff_check_20260924.py` 核对 sha256；
- **重建**：跑下表的 rebuild 命令（零平台算力，但耗时取决于机器；`built_signals.pkl` 最慢）。

| 文件 | 大小 | 重建命令 |
| --- | ---: | --- |
| `pool-screen-20260921-qualitygate3/signals.pkl` | 53 MB | `POOL_SCREEN_OUT=research_reports/platform_alignment/pool-screen-20260921-qualitygate3 python scripts/screen_competition_pool_20260921.py` |
| `pool-extended-search-20260922/built_signals.pkl` | 228 MB | `python scripts/prescreen_extended_members_20260922.py`（首次会建 54 个成员面板，最慢） |
| `alpha191-local-20260923/panels.pkl` | 477 MB | `python scripts/alpha191_panel_build_20260923.py`（无跳过逻辑，会覆盖） |
| `seat-rescreen-20260924/fnet01_panels.pkl` | 8.4 MB | `python scripts/pool_seat_rescreen_20260924.py`（缺失时自动重建） |
| `pool-screen-20260921/signals.pkl` | 57 MB | 旧目录对照，可选项 |

机器可读清单（含 sha256、生成脚本、依赖这些缓存的脚本列表）：
`research_reports/platform_alignment/handoff_20260924/local_data_manifest.json`。

这些缓存的关系是链式的：`signals.pkl` → `built_signals.pkl`（再由 Alpha191/宽字段 GP 筛选读取）。
缺失时**只有**依赖它的脚本会报错，仓库里的 CSV/JSON/MD 结果不受影响，可以先读结论、后补缓存。

`quantlab/.quantlab/cache` 约 4.5 GB 的行情/财务缓存不在这里列出：其中约 0.8 GB 走 Git LFS 快照
（`data/local_recheck/factor-local-recheck-data.tar.gz`），按上面第 0 节导入即可。

## 3. 本轮（2026-09-24）研究状态

**宽字段 GP 挖掘 + 放宽换手重筛，全部本地零平台算力**，结论目录：
`research_reports/platform_alignment/relaxed-gp-20260924/`。

- 8 个 GP run、42,293 条公式，健康 40,511 条（`records_healthy.csv`）；
- 重算筛选 253 个候选（`screen2/screened_candidates.csv`）；
- 主要结论：≤30% 换手档的净额被 size 订死（`corr_size` 0.99–1.00，去 size 对照从 25.2% 掉到 20.4%）；
  放宽到 40–60% 换手才出现 size 无关的新东西（`corr_size` 0.03–0.33，近段 2026 净额最高 27.7%）；
- 253 个候选作第 6 席的池级 ΔComb 全为负（最好 −2303 分/月），按新口径只作参考列；
- 现役 5 席（平台 ID）：
  SIZE `6aa3b8573e7967143f8faf29`、H03 `6aa36bd3cffa1665a2101716`、
  T10-ADD-BM `6aa3c6d1a7f535324660ba70`、VERIFY10-E（BM−SIZE）`6aa28e090f6165ec8f7f378b`、
  VERIFY10-F（BM+impact）`6aa28e555d52c44d2f55c2dc`；
- 池基线 `F-P260922-08`：净 20.30%、换手 13.39%/次、Sharpe 1.0648、MaxDD 31.64%、RankIC 0.0919、ICIR 0.3675。

修过的两个真卡点（已入库）：字段失败登记表版本错位（现可用 `--allow-stale-failure-registry` 显式放行）、
命名字段面板缓存只有 24 槽导致 GP 出不了代（现拆成 CPU 数组缓存 + GPU 面板缓存两层）。

## 4. 未决事项（新机接手第一件事）

1. **4 条候选是否上平台验证**：AGG / G13 / DOWNSIDE / WC（6 席池实测 ΔComb +148.8 / +148.3 / +108.7 / −783.3 分/月）。
   已按项目规矩报候选与预算（每条 4 算力，共 16，余额 1752.12），**等用户明确批准**，不要自行提交。
2. 若批准：用 `python scripts/pandaai.py batch <candidates.txt> --start 20210907 --end 20260907 --cycle 10 --group-number 10 --round-trip 0.003 --prefix ... --max-runs 1`，一次 1 条候选。
3. 换手门槛仍按用户新规：以最终 ΔComb（NC 为主）排序，≤30% 为主范围，30–40% 单独保留，>40% 单列；
   继续保留 size 防作弊列（`corr_size`、桶内 size 中性化 `neu_net`），不要退回"低换手即好因子"。

## 5. 已知的坑

- `.gitignore` 忽略 `*.pkl`：**不要**为了交接把这些大文件提交进仓库（GitHub LFS 免费额度 1 GB，已被
  541 MB 快照占用；再塞 780 MB 会超）。要带走就直拷，或按第 2 节重建。
- 平台 token、密码、API Key 一律不入库；新机必须重新登录。
- `quantlab` 子模块在旧机是 detached HEAD（`9cd90d4`）且有未提交脏改动，**未交接**；新机拿到的是干净版本。
- 平台计费有延迟，余额表按查询值回填；上平台前重新查一次 `balance`。
