# 项目目标：PandaAI 第四届因子大赛 年度综合积分榜第一名

> 建立日期：2026-09-23 ｜ 基线快照：2026-09-22 preview（276 个池，见
> [`research_reports/platform_alignment/goal-20260923/leaderboard_points_20260922.csv`](research_reports/platform_alignment/goal-20260923/leaderboard_points_20260922.csv)）

## 一、目标与验收

- 主目标：**年度综合总榜第一**。年度榜 = 2026-09 起各月月末结算积分直接累加，缺月不补 0，
  2027 年 9 月上旬定奖（冠军 20 万，前 50 名分 70 万）。
- 不是"单月第一"，也不是"某个子榜第一"；验收看年度累计积分。
- 我们 2026-09-22 建池、2026-09-24 首次统一调仓，9 月基本挂零，起步落后一个月。

## 二、现状基线（2026-09-23）

池「mingle」（综合型、10 日统一调仓、待生效）：SIZE-ONLY / H03-T10 / VERIFY10-E /
VERIFY10-F / T10-ADD-BM。

| 指标 | 数值 | 来源 |
| --- | --- | --- |
| 平台实测净超额（5 年） | 20.30% | 平台池回测 F-P260922-08 |
| 每次调仓换手 | 13.39%（月 ≈28.6%） | 同上 |
| 夏普 / MaxDD | 1.0648 / 31.64% | 同上 |
| RankIC / ICIR / 单调性 | 0.0919 / 0.3675 / 0.99 | 同上 |
| 席位 S_i（平台口径） | 0.0102 / 0.0158 / 0.0169 / 0.0154 / 0.0373 | 平台单因子 |
| rawA → NA | 0.0191 → **0.239**（新池上限 0.70） | 本地代理，对平台 corr 0.982 |
| NC 预期 | 常态月 1.00；首月（建仓计 0.5 月换手）≈0.68；三调仓月 ≈0.84 | 本地代理 |
| NB | 当前 0；首条样本外 IC 在纳入后的第二个统一调仓日（国庆后），此后每 10 个交易日 1 条 | 规则 |
| 新人加成 | 月度积分 ×1.10（注册后 90 天内） | 规则 |

## 三、与第一名的差距（核心）

月度积分 = `min(Comb × 40000 × 加成, 40000)`，`Comb = 0.20 NA + 0.35 NB + 0.45 NC`。

> **评分公式已于 2026-09-24 用 283 池原生快照验证到机器精度**（残差 1e-16 级，
> `research_reports/platform_alignment/nc-turn-20260924/summary.md`）：
> `NA = min(rawA/0.08, 0.70)`（新生效 <6 月封顶 0.70）、`NB = min(rawB/0.06, 1.0)`、
> `NC = min(rawC/0.6, 1.0)`，其中
> **`rawC = max(Rex_ann,0) / max(Turnover_month, 0.30) × SR_ann × (1 − 1.2·MaxDD_month)`**。
> 纯分数汇率：`0.01 NA = 88 分/月`、`0.01 NB = 154`、`0.01 NC = 198`（加成后），
> 即 **NC : NB : NA = 2.25 : 1.75 : 1.0**——牺牲 NC 换 NA 的汇率是 2.25:1。

以 NA 0.239、NC 1.00 为基准折算：

| NB | 折算积分 | 当前榜上位置 |
| ---: | ---: | --- |
| 0 | 21,903 | ≈第 24 名 |
| 0.3 | 26,523 | ≈第 4 名 |
| 0.5 | 29,603 | 第 4 名档 |
| 1.0 | 37,303 | 第 1 名档 |

对手与分数线（2026-09 快照）：第 1 名 35,517（NA 0.036 / NB 1.00 / NC 1.00）、
第 2 名 34,980、第 3 名 30,318、第 4 名 25,917、第 10 名 23,047、第 20 名 22,121。

**结论：超过现榜首需要 Comb ≥ 0.807，即在 NA 0.239、NC 1.00 下 NB ≥ 0.85；
或走混合路线 NA 0.5 + NB 0.5（≈34,100）、NA 0.7 + NB 0.4。**

时间账：榜首 9 月已进账 35,517，我们 9 月基本为零。要在年度榜反超，未来 11 个月
平均每月需比对手多约 **3,200 分**（≈Comb +0.073），且不能出现冻结月。

全榜分布（276 个池）：NC=1 的有 72 个（前 ~75 名的门票）；NB>0 仅 45 个、NB=1 只有
2 个；NA 中位 0.148、p90 0.333、上限 0.70（新池封顶）。**分差几乎全部来自 A 与 B。**

## 四、已知结构约束（已证伪的路线，不要再重复投入）

1. **A 与换手同向**：库内 3,785 个合成候选都落在 `S_i ≈ 0.12–0.17 × 每次调仓换手`
   的边界上。换手 ≤14%/次 的候选最高 S_i 只有 0.0193；要 S_i ≥0.038 需要 ≥30%/次。
   2026-09-23 又用本地 AlphaPROBE GP 以"单位换手效率"为目标挖了 4,600 个公式
   （`research_reports/platform_alignment/ic-efficiency-mining-20260923/summary.md`）：
   换手 ≤10% 的最高 S_i 仍是 0.019-0.022，且入选候选与 H03/impact 的截面相关
   0.88-0.93——**是同一根轴的复制，不是新信号族**。A 分项按已饱和处理。
   2026-09-23 又把公开的 Alpha191 全部 191 条本地复现（`research_reports/platform_alignment/
   alpha191-local-20260923/summary.md`）：183 条跑通，S_i 中位 0.0045、最高 0.0303、
   每次调仓换手中位 79.7%、多头净超额中位 −20.8%，**没有一条同时满足 S_i ≥0.035 与换手 ≤10%**。
   Alpha191 这条"未探索空间"同样按不可用处理。
   同日平台实测 `cal_mins` 日内字段探针（`research_reports/platform_alignment/
   calmins-probe-20260923/summary.md`）：3 个候选里 2 个跑通、1 个执行失败；
   跑通的两个每次调仓换手 85-89%、S_i 仅 0.0004 / 0.0038（最弱一席 SIZE 是 0.0102）。
   日内字段按"只有作为复合写法的一部分才可能可用"处理，不再单独盲测。
2. **我们的池贴着换手下限（2026-09-24 修正代价）**：月换手 28.6%（2 × 14.3%），C 的分母
   地板 0.30，我们 2 调仓月 T=0.268 < 0.30 → NC 满分；3 调仓月 T=0.402 → NC 0.84。
   加席的代价只在 3 调仓月（占 11.7% 的月份）按 `ΔT/T` 线性出现，**期望代价 200~400 分/月，
   不是 2,000–3,000/月**；且新席换手 ≤34%（无余量假设下 23%）时连 2 调仓月都不掉分。详见
   `research_reports/platform_alignment/nc-turn-20260924/summary.md`。
3. **平滑无效**：对高 IC 成员做 2/3/6/10 步平滑，S_i 与换手同比例下降，单位换手效率不变。
4. **本地模型对混合池系统性高估净超额 3.5–5.9pp**：本地 Comb 只用于相对排名，
   不作为提交依据；0.02 量级的差异没有行动价值。
5. **换手低于下限不再加分**：分母锁 0.3，继续压换手只省成本，不额外得分。
6. 池统一调仓周期创建后不可修改；每位主体只能持有 1 个正式池；有效因子 <5 当月冻结；
   修改因子公式/代码会重置该因子的样本外（B）序列，只改名字不重置。
7. **高净额 ≈ size 暴露（2026-09-24 用两组独立证据确认，不要再挖"净额目标"）**：
   - 从 32,475 条历史记录里**随机抽** 192 条（非净额 top 取样）：`|corr_size| ≤0.3` 的 127 条
     平均净额 **−9.8%**，`corr_size ≥0.95` 的 10 条平均 **+21.2%**；原始净额 ≥10% 的 33 条里
     `|corr_size| ≤0.5` 的 **0 条**。
   - `full374b` 十五条从未上平台的净额候选：s901 五条全退化（主轴 `BOND_PAYABLE` 覆盖率仅
     34.7%）；s902/s903 十条中 **8 条与 `size_only` 相关 ≥0.99**，余 2 条是弱化版，
     全池模拟最好只 +11 分/月、加第 6 席全为负。
   （`research_reports/platform_alignment/size-dominance-probe-20260924/`、
   `full374b-net-seat-20260924/`）
8. **五席共享一个 size 公共因子（NC 防守风险项）**：去掉 SIZE 席只掉 0.9pp
   （净额 23.80% → 22.86%），但五席**全部**对 size 中性化后毛超额 25.96% → 7.21%、
   净额只剩 1.79%（换 N2/N3/N4 配方 4.99% / 5.41% / 4.99%，差异来自换手）；
   组合与 size 轴的截面 Rank 相关 **0.8371**。即 size 风格一旦反转，五席会同时受伤。
   中性化配方 = 逐日按 `−rank(total_mv)` 等频 20 桶、桶内去均值取残差。
9. **"size 中性化净额"目标同样没找到货，且原因定位在目标函数（2026-09-24）**：
   - 最优一条 +13.2% 净额**只存在于"20 桶去均值后的面板"**上；平台按原始因子值排序，
     其**原始**版本 RankIC = +0.004、两个方向毛超额都是负的（净 −1.5%~−4.6%）→ 不可提交。
   - 它也不是"和 size 无关"：原始与 size 秩相关 +0.17，**去均值后 +0.51**；
     把残差的 size **秩序**剥掉后毛超额 +19.1% → 全局剥 +4.4% / **桶内剥 −1.8%**（IC +0.014）。
   - **根因：桶内按"均值"去中性可被重尾极值击穿**（|value| p50=0.80、max=1.1e4）——
     桶均值被钉在 0（残差线性相关 ≈0），主体仍按 size 排序（秩相关 +0.51）。
     **不要再跑"均值去中性"的目标；要做得用桶内中位数/秩去中性，或对原始面板加 |corr_size| 硬约束。**
   - 其它已判死：b40/b100 更细桶（IC 仍 +0.036~0.040、与 size 秩相关 +0.40~+0.48、
     毛 +12%~+16% 但换手 0.48~0.63、净额塌掉）；最优族极自相似（净额 top100 全含
     `Less(OPER_DU_ROE_LYR, …)`）；`S_i ≈ 0.004`、换手 0.34、加第 6 席 **−270 分/月**；
     **净额 ≥10% 且换手 ≤15% 的 0 条**。
   （`research_reports/platform_alignment/sizeneut-gp-20260924/summary.md`）

10. **第 6 席重筛（2026-09-24，零算力）——门槛量错了对象，且正收益全部带 size**：
   - **DD 口径修正（最重要）**：平台 C 分项用的是"**当月**日频 MaxDD"（283 池中位 4.15%），
     不是全期 DD。新建日频账本（`scripts/pool_daily_ledger_dd_20260924.py`）实测五席
     **月度日频 MaxDD 中位 4.59%**（p75 8.51% / p90 12.47%）→ 之前用全期 27.4% 算出的
     "加席必亏 −1,400~−2,600 分/月"是**错口径**，只能当压力测试。
   - **换手门槛量错了对象**：进 C 分项的是**加席后的池换手**。席位自身换手 27.2% 的 t10 族
     只把池换手从 14.30% 抬到 **17.39%/次**（+21%）。旧 "≤10%" 与现 "≤30%" 都是在卡一个
     不该单独卡的量。
   - **重筛结果（51 条本地候选，按最终 ΔComb 排序）**：月均为正的只有 6 条，**全是
     `t10_size_plus_impact` 族**（+291 ~ +476 分/月，K5 逐月实测），且 **corr_size 0.557~0.711
     全部过不了 size 防作弊线**；`|corr_size| ≤ 0.5` 的 39 条里**没有一条月均为正**，
     最好的 `T10-NOMCAP-PLUS-IMPACT` 月均 −15（中位 +701、p10 −2,371、18% 月份为负）= 零期望。
     换手 >40% 的 16 条全负（−676 ~ −12,000 分/月）。
   - **t10 族的正贡献全部来自 ΔNB**（+600~690 分/月，因为 S_i 0.033~0.035 > 现役均值 0.0191）：
     盈亏平衡只需它未来 S_i^B ≥ 0.0175~0.0181（历史值的一半），赢面大但不是白捡。
   - **F-NET01 当第 6 席是负贡献**：K5 −1,806 分/月（97% 月份为负；K1/K2/K3 −1,296/−542/−583）。
     它的 +12.76% size 中性化净额是真的，但 S_i 0.0130 < 现役均值 0.0191 → 同时稀释 A 与 B，
     并把池净额从 23.80% 压到 22.25%。要让它不亏需要 S_i^B = 0.0368（历史值的 2.8 倍）。
   - **池级 size 防作弊**：加 t10 族不加深池子 size（corr_size 0.837 → 0.822~0.835、size 中性化
     净额 4.97% → 4.65~5.28% 基本平）；**只有 F-NET01 改善**（+2.36pp → 7.33%）。
     即"提分"与"降 size 暴露"在当前候选集里**反向**——不是搜索范围问题。
   - **加席时点**：10-01~03 vs 晚一期的 NB 稀释期望差 **≈0**（蒙特卡洛 −729±413 分/12 个月；
     单期稀释仅 23~85 分，因为早期 rawB 常被 0.06 上限吸收）。旧"要加就趁 10-01~03"的理由不成立。
   - 附带：**NB 会先高后低**（按各席历史 IC 模拟 M01 ≈ 0.97 → M06 ≈ 0.58 → M12 ≈ 0.43 → 收敛 ≈ 0.32），
     别把首月 NB 当常态。
   （`research_reports/platform_alignment/seat-rescreen-20260924/summary.md`、
   `daily-dd-20260924/`、`nb-dilution-20260924/`）
   **平台确认（16 算力）**：AGG/G13/DOWNSIDE/WC 四个 6 席池全部实测成功。用平台全期
   MaxDD 代理桥，边际为 **AGG +149 / G13 +148 / DOWNSIDE +109 / WC −783 分/月**；
   四条都通过池级 size 防作弊，但 AGG/G13/DOWNSIDE 实测比 K5 低约 260~300 分/月，
   且仍是同一 `t10_size_plus_impact` 族，只能加一席、不能叠加。
   （`platform_pool_tests_20260924/summary.md`、`platform_pool_tests_20260924/platform_bridge.json`）

11. **宽字段 GP 挖掘 + 放宽换手重筛（2026-09-24，零平台算力）——低换手净额确实被 size 订死，放宽换手才有新东西**：
   - 先修两个工程卡点：①字段失败登记表版本错位（`qualitygate3` vs `qualitygate4`）导致所有
     GP 起不来；②命名字段面板缓存只有 24 槽，而单字段首次物化 **>3 秒** → 1,152 字段满预算
     （pop 1000 × 40 代）跑 25 分钟出不了 1 代。改成 CPU 数组缓存（512）+ GPU 面板缓存（48）
     两层后，374 字段同预算约 30 分钟跑完（含一次性冷启动）。
   - 字段集从 **19 个 verified 扩到 374 个**，8 个 run、**42,293 条公式**；另做 **357 字段
     去 size 对照**（删掉全部 17 个市值/股数字段）。
   - **对照结论（回答"净额是不是被 size 订死"）**：≤30% 换手档净额前列 `corr_size` 0.99–1.00；
     删掉 size 字段后该档最好记录 **25.2% → 20.4%**，且仍是"账面价值 ÷ 成交额缩放"这类隐式
     size 替代。**低换手区净额上限由 size 轴决定，不是搜索范围不够。**
   - **放宽换手到 40–60% 才有新东西**：`corr_size 0.33` 且近段 2026 净额 **27.7%** 的候选；
     以及 `corr_size 0.03` 的 size 无关候选（代价：全期净额仅 4.8%、池级 ΔComb 仍为负）。
   - 253 个候选**逐个按 GP 内部同一套组合构造/换手/成本重算**（记录里的 turnover 在 size 中性化
     目标下是中性化后的值，直接分档会把中性化噪声当低换手）；五席基线复算回到
     Comb 0.4978 / 净额 23.8%，口径自检通过。
   - 253 条作第 6 席的池级 ΔComb **全为负**（最好 −2,303 分/月）——按用户新口径只作参考列。
   - 待办：候选多为 catalog/未验证字段名，需平台验证；已报 4 条候选 × 4 算力待批准。
   （`research_reports/platform_alignment/relaxed-gp-20260924/summary.md`、
   `screen2/screened_candidates.csv`、`records_healthy.csv`）

## 五、唯一可主动改变的杠杆

| 杠杆 | 分数当量 | 可控性 |
| --- | ---: | --- |
| NB 0 → 1 | +15,400 分/月 | 只能靠因子真实样本外表现 + 时间；动作要求"不删、不改、≥5 只有效因子" |
| NA 0.239 → 0.70 | +4,048 分/月 | 需要**新信号族**：高 IC（换手放宽到 ≤30%/次，见 §6.2） |
| NC | 已是 1.00 | 只防守：月换手别越 0.30、月回撤别放大（三调仓月约 −3,100 分） |

## 六、决策规则（本项目默认执行）

1. 不修改已提交池内 5 只因子的公式/代码；不删除已生效席位（避免 B 序列清零）。
2. 新席位准入门槛（2026-09-24 二次修订，换手项改为**池级**）：10 日周期 `S_i ≥ 0.035`、
   5 年净超额 `≥15%`、与现有席位相关性低；**判据用"加席后的池换手"而不是席位自身换手**
   （席位自身换手只作参考：≤30% 优先，30~40% 看最终 ΔComb，>40% 一票否决——本轮 >40% 档 16 条全负）。
   加席前必须用本地组合模拟实测：① 加席后 `T = k×池换手` 不超过 `T* = Rex·SR·(1−1.2DD)/0.6` 的 1.15 倍
   （`k` 用当月真实调仓次数 1/2/3）；② **池级 size 检查**：加席后池子 size 中性化净额不得下降、
   `corr_size` 不得上升；③ 逐月实测 ΔComb 月均 > 0（K5 口径，Rex/SR 用月度日频账本）。
   先本地预筛，够线才提平台实测。（旧值 ≤10% 只值 +86 分/月；6 席免费区上限解析解 34.2%、
   无余量假设 23.0%，见 `nc-turn-20260924/summary.md`；本轮重筛见 `seat-rescreen-20260924/summary.md`）
3. 平台回测按项目规矩：先列候选公式、数量、算力预算与目的，用户批准后每轮 ≤3 个
   （每个候选 4 算力）；结果先报告，不自动开第二轮。
4. 10 月 1–3 日的因子删改窗口是唯一低成本换人窗口（B 尚未累积）；但 2026-09-24 实测
   **加席时点的 NB 稀释差 ≈ 0**，选窗口不必赶，按准入门槛过了再动。
5. 每月月末结算后更新本文件"现状基线"与"差距"两节。
6. **换/加席位的三段账（口径已固化，`score-v2-seatbridge-20260924`）**：席位变动同时动三段——
   **ΔA 立即重算**（席位自带 120 期 RankIC 历史，`Δraw_a = Δ均值`，`分/月 = Δraw_a/0.08×8800`）、
   **ΔB 稳态**（池龄≥1 月，`Δraw_a/0.06×15400`）+ **一次性首期稀释**（新席样本外序列从 0 起、被换掉席位历史作废）、
   **ΔC 重新定价**（`Rex/max(T,0.30)×SR×(1−1.2DD)` 的前后差，换手分母前 0.30 免费）。
   `raw_a` 是算术平均 ⇒ **换掉弱席比加同分新席强约 1.8 倍**；月换手 0.30→0.50 在我们水平下 = −6,320 分/月，
   能一口吃掉 A+B 的全部增益。**排序键只能是 ΔComb（三段之和），任何单段都不能当结论**；
   实现见 `scripts/platform_score_rules.py`（`seat_change_bridge` / `seat_change_turnover_points`），
   口径与算例见 `research_reports/platform_alignment/SCORE_RULES.md`，回归用 `--self-test`。

### 平台算力预算纪律（2026-09-23 追加，用户要求"省着点用，要钱的"）

- **默认零算力**：所有挖掘、筛选、组合模拟、稳定性分析一律在本地做；本地能回答的问题
  不上平台。
- **平台只用于两件事**：① 对本地筛出的**决赛候选**做确认；② 必要时做字段对齐校验。
- **单轮预算 ≤ 12 算力**（3 个候选 × 1 窗口，或 1 个候选 × 三窗口），且必须先报候选与预算、
  经用户批准。
- **筛选顺序**：本地三窗口 → 本地逐月稳定性 → 平台五年（4 算力）→ 只有进入决赛 2 个以内
  才补近一年/近三个月（每个再 4 算力）。
- **禁止**：用平台批量试探字段、用平台做"看看效果"的探索、对本地已否决的方向再上平台验证。
- 余额与消耗记在下面，每次平台运行后更新。

| 日期 | 事项 | 消耗 | 余额 |
| --- | --- | ---: | ---: |
| 2026-09-23 开盘 | 当日开始前余额 | — | 1450.12 |
| 2026-09-23 | cal_mins 三个探针（2 成功 1 失败，失败未计费） | 8 | 1442.12 |
| 2026-09-23 | 三窗口实测 6 次 + 8 席 2 次 + 候选1 补测 2 次 + 6 席三窗口 3 次 | ~34 | 1408.12 |
| 2026-09-23 收盘 | 当日合计 | **~42** | **1408.12** |
| 2026-09-24 | 4 个过闸候选的 6 席池实测（AGG/G13/DOWNSIDE/WC） | 16 | 1752.12 |
| 2026-09-24 | 宽字段 GP 挖掘（374 字段）+ 放宽换手重筛（全本地） | 0 | 1752.12 |

（平台计费有延迟，表内按查询到的余额回填；每次运行后应复核。）

## 七、观测检查点

| 时间 | 检查项 |
| --- | --- |
| 2026-09-24 | 首次统一调仓：确认建仓把首月月换手记为 ~0.5（验证换手口径） |
| 2026-10-01~03 | 因子删改窗口（唯一低成本换人窗口） |
| 2026-10 上旬 | 首条样本外 IC：平台 NB 首次非零，核对与本地代理的方向；预期偏高（小样本 ICIR 高估） |
| 2026-10 首份快照 | 用 `rawC = Rex_ann/max(turn,0.3)×SR×(1−1.2·MaxDD_month)` 反解我们池子的月度 inputs，核对日频账本代理（月度 DD 中位 4.59%） |
| 每月月末 | 若月度 MaxDD > 5.65%，加席的净贡献在**当月**转负（表 3 的 DD 盈亏平衡线） |
| 2026-10 月末 | 首次正式结算：核对平台 NA/NB/NC 与本地代理，校准模型误差 |
| 每月末 | 更新基线、更新与榜首的分差、复核换手是否越过 0.30 |
