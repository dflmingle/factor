# T10-SIZE vs H03-T10-SINGLE Correlation

- Analysis workflow: `6aa371b551cdfe29b2e0bc34`
- Run: `6aa371b5ecb163ea7228d004`
- Correlation task: `3e07308fd6664f1096e128d2bc6b70c3`
- Window: `2021-09-07` to `2026-09-07`
- Method: PandaAI `FactorCorrelationCalculationControl`
- Actual compute deduction: 2 credits (`620.120152` -> `618.120152`)

## Inputs

### T10-SIZE baseline

```text
(RANK(1-RETURNS(CLOSE,40)) + RANK((SUM(VOLUME*(OPEN+CLOSE)/2,250)/SUM(VOLUME,250))/CLOSE-1) + RANK(1-MA(TURNOVER,21)/MA(TURNOVER,504)) + RANK(-ZSCORE(RANK(MARKET_CAP)))) / 4
```

### H03-T10-SINGLE

```text
RANK(SUM((HIGH-LOW)/(DELAY(CLOSE,1)+0.000001),60)/(SUM(AMOUNT,60)+1))
```

## Result

| | T10-SIZE | H03-T10-SINGLE |
|---|---:|---:|
| T10-SIZE | 1.0000 | 0.4151 |
| H03-T10-SINGLE | 0.4151 | 1.0000 |

The platform factor-correlation result is `0.4151352929`. This is moderate rather than high
redundancy, and is below the project's rough `0.6` threshold for treating two factors as largely
the same bet. It supports keeping H03 as a potentially complementary sleeve, but does not prove
independent alpha or remove size/liquidity exposure concerns.

This is a correlation diagnostic on the platform's factor-correlation node, not an independent
out-of-sample validation and not evidence that a combined portfolio's return is additive.
