# Existing paper-derived composite, rerun with a 10-day rebalance cycle.
# Window: 2021-09-07 to 2026-09-07; groups: 10; one-way cost: 0.30%.

paper-derived-composite ~ ZSCORE(RANK(book_to_market_ratio_lyr))+ZSCORE(RANK(oper_roe_lyr))-ZSCORE(RANK(gr_total_asset_lyr))-ZSCORE(RANK(MARKET_CAP)) ~ 1
